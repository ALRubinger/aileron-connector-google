name = "gmail-and-google-calendar"
description = "Read and draft Gmail; read and create calendar events; send mail (incl. existing drafts) and create events with per-call approval."

actions = [
  "actions/list-recent-emails",
  "actions/get-email",
  "actions/list-upcoming-events",
  "actions/draft-email",
  "actions/send-email",
  "actions/send-draft",
  "actions/create-calendar-event",
]
